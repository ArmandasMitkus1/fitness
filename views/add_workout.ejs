<!DOCTYPE html>
<html>
<head>
  <title><%= pageTitle %></title>
</head>
<body>
  <h1>Add Workout</h1>

  <% if (error) { %><p style="color:red;"><%= error %></p><% } %>
  <% if (success) { %><p style="color:green;"><%= success %></p><% } %>

  <form method="POST" action="/add_workout">
    <label>Date:</label>
    <input type="date" name="workout_date" required><br><br>

    <label>Type:</label>
    <input type="text" name="type" placeholder="e.g., Cardio" required><br><br>

    <label>Duration (min):</label>
    <input type="number" name="duration_minutes" required><br><br>

    <label>Notes:</label>
    <textarea name="notes"></textarea><br><br>

    <button type="submit">Add Workout</button>
  </form>

  <p><a href="/logs">View Logs</a></p>
</body>
</html>
